<h1>id: <%= @book.id %>の詳細ページ</h1>

<table class="table table-bordered">
  <tr>
    <th>id</th>
    <td><%= @book.id %></td>
  </tr>
  <tr>
    <th>タイトル</th>
    <td><%= @book.title %></td>
  </tr>
  <tr>
    <th>内容</th>
    <td><%= @book.content %></td>
  </tr>
</table>

<%= link_to 'このbookを編集する', edit_book_path(@book), class: 'btn btn-default' %>
<%= link_to 'このbookを削除する', @book, method: :delete, data: { confirm: '本当に削除してよろしいですか？' }, class: 'btn btn-danger' %>
